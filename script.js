document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('nav');if(menu&&nav)menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('[data-demo-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const button=form.querySelector('button');button.textContent='Saved successfully ✓';button.disabled=true;const note=document.createElement('p');note.className='form-note';note.textContent='Thanks! This demo form is ready to connect to your backend.';form.append(note)}));const search=document.querySelector('#talent-search');const filter=document.querySelector('#talent-filter');const cards=[...document.querySelectorAll('.talent-card[data-search]')];function update(){const query=(search?.value||'').toLowerCase();cards.forEach(card=>{card.style.display=card.dataset.search.includes(query)?'':'none'})}if(search)search.addEventListener('input',update);if(filter)filter.addEventListener('change',update)});
